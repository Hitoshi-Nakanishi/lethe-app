"""Audio capture utilities.

- ``recorder`` / ``cli``: Windows WASAPI speaker loopback CLI (`python -m audios`).
- ``lethe``: Lethe, the cross-platform Tkinter recorder / transcriber /
  minutes app (`lethe` or `python -m audios.lethe`).
- ``preprocess``: bandpass + noise-reduction pipeline shared by Lethe.
- ``settings``: persisted Lethe preferences and temp-file cleanup.
"""
